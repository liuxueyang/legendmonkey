#!/usr/bin/perl -wn
/^\s*($|#(?!!))/||print;

# this is a program...

#while (<>)
#{
    # hello, I am comment...
#    /^\s*#/
#print unless /^\s*#/ && !/^#!/ || /^\s*$/;
#print unless /^\s*$/ || /^\s*#(?!!)/;
#print unless /^\s*($|#(?!!))/;

   #! I am a comment..
#}


